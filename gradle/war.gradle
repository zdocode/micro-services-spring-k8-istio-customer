apply plugin: "war"

bootWar {
    mainClassName = "com.zyllion.sigma.customer.CustomerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
